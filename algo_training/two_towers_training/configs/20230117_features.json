{
  "user_embedding_layers": {
      "user_id": {"type": "string", "embedding_size": 64},
      "user_age": {"type": "int", "embedding_size": 8}
  },
  "item_embedding_layers": {
      "item_id": {"type": "string", "embedding_size": 64},
      "offer_categoryId": {"type": "string", "embedding_size": 8},
      "offer_subcategoryid": {"type": "string", "embedding_size": 8}
  }
}
