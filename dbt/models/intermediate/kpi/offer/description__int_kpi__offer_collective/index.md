KPI table aggregating created collective offers by month and venue geography.

Each row represents a `partition_month` / venue geography combination. The model is built from [`int_global__collective_offer`](#!/model/model.data_gcp_dbt.int_global__collective_offer).
