from utils import ENV_SHORT_NAME

params = {
    "semantic_content": {
        "dataset_name": f"clean_{ENV_SHORT_NAME}",
        "table_name": "item_embeddings",
        "column_name": "semantic_content_hybrid_embedding",
    }
}
