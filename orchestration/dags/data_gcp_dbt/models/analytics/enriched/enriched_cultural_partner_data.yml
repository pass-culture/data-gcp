version: 1

models:
  - name: enriched_cultural_partner_data
    config: 
      schema: analytics_{{ target.name }}
      materialized: table
      tags: ["pro"]
    columns:
      - name: venue_id
        tests: 
          - unique



