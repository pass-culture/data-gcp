version: 2

models:
  - name: exp_backend__artist_score
    description: "{{ doc('description__exp_backend__artist_score') }}"
    columns:
      - name: artist_id
        description: "{{ doc('column__artist_id') }}"
        data_tests:
          - not_null
          - unique
      - name: artist_app_search_score
        description: "{{ doc('column__artist_app_search_score') }}"
        data_tests:
          - not_null
      - name: artist_pro_search_score
        description: "{{ doc('column__artist_pro_search_score') }}"
        data_tests:
          - not_null
