select
    id as external_poi_id,
    nom_du_lieu,
    nom_usuel_du_lieu,
    entite_juridique_rattachement,
    siret as poi_siret, -- not unique and not always present
    adresse_complete,
    adresse_entree_public,
    numero_et_libelle_voie,
    complement_adresse,
    cast(code_postal as string) as code_postal,
    commune,
    concat({{ normalize_address("numero_et_libelle_voie", "cast(code_postal as string)", "commune") }}, ' ', lower(cast(code_postal as string)), ' ', lower(commune)) as normalized_poi_address,
    concat({{ normalize_address("adresse_entree_public", "cast(code_postal as string)", "commune") }}, ' ', lower(cast(code_postal as string)), ' ', lower(commune)) as normalized_poi_public_address,
    autres_communes,
    code_commune,
    code_arrondissement,
    departement,
    code_departement,
    region,
    code_region,
    pays,
    latitude,
    longitude,
    site_internet_et_autres_liens,
    telephones,
    courriels,
    domaines,
    domaines_noms_publics,
    sous_domaines,
    sous_domaines_noms_publics,
    types,
    types_noms_publics,
    labels,
    labels_noms_publics,
    annee_obtention,
    description as poi_description,
    images,
    mentions_legales,
    auteur_nom_illustre,
    identifiant_deps,
    identifiant_origine,
    source_origine,
    accessible_au_public,
    conditions_ouverture,
    livraison,
    retraits_sur_place,
    accueil_public_sur_rendezvous,
    conditions_acces,
    date_creation,
    date_modification,
    coordonnees_geographiques_lieu,
    coordonnees_geographiques_commune_lieu
from {{ source("seed", "base_lieux_culturels_ouverts") }}