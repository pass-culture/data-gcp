version: 2

models:
  - name: int_global__booking
    config:
      +materialized: "{{ var('CI_MATERIALIZATION', 'view') }}"
  - name: int_global__collective_offer
    config:
      +materialized: "{{ var('CI_MATERIALIZATION', 'table') }}"
  - name: int_global__offer
    config:
      +materialized: "{{ var('CI_MATERIALIZATION', 'table') }}"
  - name: int_global__offerer
    config:
      +materialized: "{{ var('CI_MATERIALIZATION', 'table') }}"
  - name: int_global__stock
    config:
      +materialized: "{{ var('CI_MATERIALIZATION', 'table') }}"
  - name: int_global__user
    config:
      +materialized: "{{ var('CI_MATERIALIZATION', 'table') }}"
  - name: int_global__venue
    config:
      +materialized: "{{ var('CI_MATERIALIZATION', 'table') }}"
  