version: 2

models:
  - name: ml_linkage__delta_product_artist_link
    description: "{{ doc('description__ml_linkage__delta_product_artist_link') }}"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - offer_product_id
              - artist_type
    columns:
      - name: offer_product_id
        description: "{{ doc('column__offer_product_id') }}"
        data_tests:
          - not_null:
              tags: ['critical']
      - name: artist_id
        description: "{{ doc('column__artist_id') }}"
        data_tests:
          - not_null:
              tags: ['critical']
      - name: artist_type
        description: "{{ doc('column__artist_type') }}"
        data_tests:
          - not_null:
              tags: ['critical']
      - name: action
        description: "{{ doc('column__delta_action') }}"
      - name: comment
        description: "{{ doc('column__delta_comment') }}"
