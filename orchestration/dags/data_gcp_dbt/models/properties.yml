version: 2
# Temporary file during migration
models:
  # adage
  - name: typeform_adage
    config:
      schema: clean_{{ target.name }}
  # applicative folder
  - name: action_history
    config:
      schema: clean_{{ target.name }}
  - name: recredit
    config:
      schema: clean_{{ target.name }}
  - name: national_program_offer_template_link_history
    config:
      schema: clean_{{ target.name }}
  - name: bank_account
    config:
      schema: clean_{{ target.name }}
  - name: offer_validation_sub_rule
    config:
      schema: clean_{{ target.name }}
  - name: feature
    config:
      schema: clean_{{ target.name }}
  - name: mediation
    config:
      schema: clean_{{ target.name }}
  - name: venue_registration
    config:
      schema: clean_{{ target.name }}
  - name: finance_incident
    config:
      schema: clean_{{ target.name }}
  - name: internal_user
    config:
      schema: clean_{{ target.name }}
  - name: venue_educational_status
    config:
      schema: clean_{{ target.name }}
  - name: offerer_tag_mapping
    config:
      schema: clean_{{ target.name }}
  - name: offer
    config:
      schema: clean_{{ target.name }}
  - name: local_provider_event
    config:
      schema: clean_{{ target.name }}
  - name: login_device_history
    config:
      schema: clean_{{ target.name }}
  - name: deposit
    config:
      schema: clean_{{ target.name }}
  - name: criterion
    config:
      schema: clean_{{ target.name }}
  - name: finance_event
    config:
      schema: clean_{{ target.name }}
  - name: allocine_venue_provider
    config:
      schema: clean_{{ target.name }}
  - name: offer_report
    config:
      schema: clean_{{ target.name }}
  - name: product_whitelist
    config:
      schema: clean_{{ target.name }}
  - name: stock
    config:
      schema: clean_{{ target.name }}
  - name: invoice_cashflow
    config:
      schema: clean_{{ target.name }}
  - name: user_pro_flags
    config:
      schema: clean_{{ target.name }}
  - name: venue_pricing_point_link
    config:
      schema: clean_{{ target.name }}
  - name: collective_offer_template
    config:
      schema: clean_{{ target.name }}
  - name: cashflow_batch
    config:
      schema: clean_{{ target.name }}
  - name: cashflow
    config:
      schema: clean_{{ target.name }}
  - name: venue_contact
    config:
      schema: clean_{{ target.name }}
  - name: validation_rule_offer_link
    config:
      schema: clean_{{ target.name }}
  - name: cinema_provider_pivot
    config:
      schema: clean_{{ target.name }}
  - name: price_category
    config:
      schema: clean_{{ target.name }}
  - name: validation_rule_collective_offer_link
    config:
      schema: clean_{{ target.name }}
  - name: venue_reimbursement_point_link
    config:
      schema: clean_{{ target.name }}
  - name: offerer_tag
    config:
      schema: clean_{{ target.name }}
  - name: product
    config:
      schema: clean_{{ target.name }}
  - name: booking_finance_incident
    config:
      schema: clean_{{ target.name }}
  - name: cashflow_log
    config:
      schema: clean_{{ target.name }}
  - name: provider
    config:
      schema: clean_{{ target.name }}
  - name: educational_redactor
    config:
      schema: clean_{{ target.name }}
  - name: venue
    config:
      schema: clean_{{ target.name }}
  - name: user
    config:
      schema: clean_{{ target.name }}
  - name: invoice
    config:
      schema: clean_{{ target.name }}
  - name: beneficiary_import_status
    config:
      schema: clean_{{ target.name }}
  - name: collective_offer_template_domain
    config:
      schema: clean_{{ target.name }}
  - name: educational_institution
    config:
      schema: clean_{{ target.name }}
  - name: collective_stock
    config:
      schema: clean_{{ target.name }}
  - name: collective_booking
    config:
      schema: clean_{{ target.name }}
  - name: offer_validation_rule
    config:
      schema: clean_{{ target.name }}
  - name: offerer_tag_category_mapping
    config:
      schema: clean_{{ target.name }}
  - name: offerer_tag_category
    config:
      schema: clean_{{ target.name }}
  - name: educational_deposit
    config:
      schema: clean_{{ target.name }}
  - name: invoice_line
    config:
      schema: clean_{{ target.name }}
  - name: educational_domain_venue
    config:
      schema: clean_{{ target.name }}
  - name: beneficiary_import
    config:
      schema: clean_{{ target.name }}
  - name: educational_domain
    config:
      schema: clean_{{ target.name }}
  - name: venue_label
    config:
      schema: clean_{{ target.name }}
  - name: offer_criterion
    config:
      schema: clean_{{ target.name }}
  - name: offerer
    config:
      schema: clean_{{ target.name }}
  - name: pricing_line
    config:
      schema: clean_{{ target.name }}
  - name: beneficiary_fraud_review
    config:
      schema: clean_{{ target.name }}
  - name: venue_provider
    config:
      schema: clean_{{ target.name }}
  - name: payment
    config:
      schema: clean_{{ target.name }}
  - name: pricing_log
    config:
      schema: clean_{{ target.name }}
  - name: venue_bank_account_link
    config:
      schema: clean_{{ target.name }}
  - name: price_category_label
    config:
      schema: clean_{{ target.name }}
  - name: national_program
    config:
      schema: clean_{{ target.name }}
  - name: beneficiary_fraud_check
    config:
      schema: clean_{{ target.name }}
  - name: cgr_cinema_details
    config:
      schema: clean_{{ target.name }}
  - name: national_program_offer_link_history
    config:
      schema: clean_{{ target.name }}
  - name: collective_offer
    config:
      schema: clean_{{ target.name }}
  - name: venue_criterion
    config:
      schema: clean_{{ target.name }}
  - name: validation_rule_collective_offer_template_link
    config:
      schema: clean_{{ target.name }}
  - name: allocine_pivot
    config:
      schema: clean_{{ target.name }}
  - name: bank_information
    config:
      schema: clean_{{ target.name }}
  - name: educational_year
    config:
      schema: clean_{{ target.name }}
  - name: criterion_category_mapping
    config:
      schema: clean_{{ target.name }}
  - name: transaction
    config:
      schema: clean_{{ target.name }}
  - name: payment_message
    config:
      schema: clean_{{ target.name }}
  - name: collective_offer_domain
    config:
      schema: clean_{{ target.name }}
  - name: ems_cinema_details
    config:
      schema: clean_{{ target.name }}
  - name: criterion_category
    config:
      schema: clean_{{ target.name }}
  - name: boost_cinema_details
    config:
      schema: clean_{{ target.name }}
  - name: pricing
    config:
      schema: clean_{{ target.name }}
  - name: collective_offer_request
    config:
      schema: clean_{{ target.name }}
  - name: favorite
    config:
      schema: clean_{{ target.name }}
  - name: payment_status
    config:
      schema: clean_{{ target.name }}
  - name: user_offerer
    config:
      schema: clean_{{ target.name }}
  - name: trusted_device
    config:
      schema: clean_{{ target.name }}
  - name: cds_cinema_details
    config:
      schema: clean_{{ target.name }}
  - name: allocine_venue_provider_price_rule
    config:
      schema: clean_{{ target.name }}
  - name: cashflow_pricing
    config:
      schema: clean_{{ target.name }}
  - name: beneficiary_fraud_status
    config:
      schema: clean_{{ target.name }}
  # clean transformations 
  - name: booking
    config:
      schema: clean_{{ target.name }}
  - name: bookable_collective_offer
    config:
      schema: clean_{{ target.name }}
  - name: bookable_collective_offer_history
    config:
      schema: clean_{{ target.name }}

  - name: cleaned_stock
    config:
      schema: clean_{{ target.name }}
  - name: user_ip_iris
    config:
      schema: clean_{{ target.name }}
  - name: offer_extracted_data
    config:
      schema: clean_{{ target.name }}
  - name: offer_item_ids
    config:
      schema: clean_{{ target.name }}
  - name: user_suspension
    config:
      schema: clean_{{ target.name }}
  - name: available_stock_information
    config:
      schema: clean_{{ target.name }}
  - name: user_declared_iris
    config:
      schema: clean_{{ target.name }}
  - name: bookable_offer
    config:
      schema: clean_{{ target.name }}
  - name: bookable_offer_history
    config:
      schema: clean_{{ target.name }}
  - name: user_reco_iris
    config:
      schema: clean_{{ target.name }}
  - name: reimbursement_collective
    config:
      schema: clean_{{ target.name }}
  - name: reimbursement_individual
    config:
      schema: clean_{{ target.name }}
  - name: user_beneficiary
    config:
      schema: clean_{{ target.name }}
  - name: collective_offer_domain_name
    config:
      schema: clean_{{ target.name }}
  - name: user_pro_new_nav_state
    config:
      schema: clean_{{ target.name }}
  - name: user_item_consultation
    config:
      schema: clean_{{ target.name }}
  - name: item_metadata
    config:
      schema: clean_{{ target.name }}
  - name: offer_metadata
    config:
      schema: clean_{{ target.name }}
  - name: isbn_rayon_editor
    config:
      schema: clean_{{ target.name }}
  - name: firebase_aggregated_daily_campaign_events
    config:
      schema: analytics_{{ target.name }}
  - name: mrt_collective__adage_log
    config:
      schema: analytics_{{ target.name }}
  - name: mrt_native__event
    config:
      schema: analytics_{{ target.name }}
  - name: mrt_global__venue
    config:
      schema: analytics_{{ target.name }}
  - name: aggregated_monthly_user_used_booking_activity
    config:
      schema: analytics_{{ target.name }}
  # enriched folder 
  - name: enriched_booking_data
    config:
      schema: analytics_{{ target.name }}
  - name: enriched_collective_booking_data
    config:
      schema: analytics_{{ target.name }}
  - name: enriched_collective_offer_data
    config:
      schema: analytics_{{ target.name }}
  - name: enriched_deposit_data
    config:
      schema: analytics_{{ target.name }}
  - name: enriched_monthly_bookable_item_discoverability
    config:
      schema: analytics_{{ target.name }}
  - name: enriched_offer_data
    config:
      schema: analytics_{{ target.name }}
  - name: enriched_offerer_data
    config:
      schema: analytics_{{ target.name }}
  - name: enriched_offer_metadata
    config:
      schema: analytics_{{ target.name }}
  - name: enriched_offerer_tags_data
    config:
      schema: analytics_{{ target.name }}
  - name: enriched_user_data
    config:
      schema: analytics_{{ target.name }}
  - name: enriched_user_offerer
    config:
      schema: analytics_{{ target.name }}
  - name: user_item_discovery
    config:
      schema: clean_{{ target.name }}
  - name: firebase_daily_offer_consultation_data
    config:
      schema: analytics_{{ target.name }}
  - name: enriched_venue_data
    config:
      schema: analytics_{{ target.name }}
  - name: enriched_venue_tags_data
    config:
      schema: analytics_{{ target.name }}
  - name: enriched_cultural_partner_data
    config:
      schema: analytics_{{ target.name }}
  - name: firebase_aggregated_offers
    config:
      schema: analytics_{{ target.name }}
  - name: firebase_aggregated_users
    config:
      schema: analytics_{{ target.name }}
  - name: firebase_app_experiments
    config:
      schema: analytics_{{ target.name }}
  - name: firebase_bookings
    config:
      schema: analytics_{{ target.name }}
  - name: firebase_home_events
    config:
      schema: analytics_{{ target.name }}
  - name: firebase_home_funnel_conversion
    config:
      schema: analytics_{{ target.name }}
  - name: firebase_session_origin
    config:
      schema: analytics_{{ target.name }}
  - name: firebase_venue_data
    config:
      schema: analytics_{{ target.name }}
  - name: firebase_visits
    config:
      schema: analytics_{{ target.name }}
  - name: int_firebase__native_event
    config:
      schema: analytics_{{ target.name }}
  - name: aggregated_daily_user_used_activity
    config:
      schema: analytics_{{ target.name }}
  - name: user_penetration
    config:
      schema: analytics_{{ target.name }}
  - name: user_penetration_cohorts
    config:
      schema: analytics_{{ target.name }}
  - name: enriched_institution_data
    config:
      schema: analytics_{{ target.name }}
  - name: eple_aggregated
    config:
      schema: analytics_{{ target.name }}
  - name: partner_type_bookability_frequency
    config:
      schema: analytics_{{ target.name }}
  - name: siren_data
    config:
      schema: analytics_{{ target.name }}
  - name: enriched_venue_provider_data
    config:
      schema: analytics_{{ target.name }}
  - name: cultural_sector_bookability_frequency
    config:
      schema: analytics_{{ target.name }}
  - name: venue_siren_offers
    config:
      schema: analytics_{{ target.name }}
  - name: institution_locations
    config:
      schema: analytics_{{ target.name }}
  - name: firebase_similar_offer_events
    config:
      schema: analytics_{{ target.name }}
  - name: enriched_local_authority_data
    config:
      schema: analytics_{{ target.name }}
  - name: enriched_partner_activation_activity
    config:
      schema: analytics_{{ target.name }}
  - name: stock_booking_information
    config:
      schema: analytics_{{ target.name }}
  - name: enriched_stock_data
    config:
      schema: analytics_{{ target.name }}
  - name: enriched_reimbursement_data
    config:
      schema: analytics_{{ target.name }}
  - name: enriched_suivi_dms_adage
    config:
      schema: analytics_{{ target.name }}
  - name: aggregated_daily_offer_consultation_data
    config:
      schema: analytics_{{ target.name }}
  - name: enriched_partner_retention_data
    config:
      schema: analytics_{{ target.name }}