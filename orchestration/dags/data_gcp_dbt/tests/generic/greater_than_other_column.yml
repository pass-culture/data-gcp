version: 2

tests:
  - name: greater_than_other_column
    description: |
      ## It compares two column and specifically it tests if column_name is greater than other_column

    arguments:
      - name: column_name
        description: Column supposed to be the greatest.
      - name: other_column
        description: Column supposed to be the lowest.
