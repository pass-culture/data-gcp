version: "3.7"

services:
  testdb:
    image: kartoza/postgis:12.4
    ports:
      - "5433:5432"
    environment:
      - POSTGRES_PASS=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_DBNAME=db
